# Base image with JDK 21 (official, slim image)
FROM eclipse-temurin:21-jdk-alpine AS base

# Set the working directory in the container
WORKDIR /app

# Copy the built JAR from your Maven build into the container
COPY target/*.jar app.jar

# Expose the port your app will run on (Spring default is 8080)
EXPOSE 8080

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]